# Sparql queries go here

queries:
    -   name: "Writers"
        description:
        discovery_map:
            - name
            - creator
            - about
        query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?entity ?date ?name
            WHERE {
              ?entity ont:birthDate ?date .
              ?entity a dbpedia-owl:Writer .
              ?entity foaf:name ?name
              FILTER(
                ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) &&
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )&&
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
              )
            }
    -   name: "Athletes"
        description:
        discovery_map:
            - name
            - creator
            - about
        query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?entity ?name ?date
             WHERE {
              ?entity ont:birthDate ?date .
              ?entity a dbpedia-owl:Athlete .
              ?entity foaf:name ?name
              FILTER(
                ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) &&
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )&&
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
              )
            }

    -   name: "Politicians"
        description:
        discovery_map:
            - name
            - creator
            - about
        query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?entity ?name ?date  WHERE {
              ?entity ont:birthDate ?date .
              ?entity a dbpedia-owl:Politician .
              ?entity foaf:name ?name
              FILTER(
                ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) &&
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )&&
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
              )
            }

    -   name: "Musicians"
        description:
        discovery_map:
            - name
            - creator
            - about
        query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?entity ?name ?date  WHERE {
              ?entity ont:birthDate ?date .
              ?entity a dbpedia-owl:MusicalArtist .
              ?entity foaf:name ?name
              FILTER(
                ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) &&
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )&&
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
              )
            }

    -   name: "Countries"
        description: "Countries founding days"
        discovery_map:
            - name
        query: |
            SELECT ?entity ?name ?date WHERE {
                ?entity a dbpedia-owl:Country.
                ?entity a yago:Country108544813.
                ?entity dbpedia-owl:foundingDate ?date.
                ?entity foaf:name ?name
                FILTER(
                    ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) &&
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )&&
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
                )
            }

    -   name: "Books"
        description: "Book titles published on the selected date."
        discovery_map:
            - name
            - uniformName
            - about
        query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?entity ?name ?date WHERE {
                ?entity a ont:Book;
                      ont:publicationDate ?date;
                      foaf:name ?name .
                FILTER(
                   ((datatype(?date) = xsd:date) || (datatype(?date) = xsd:dateTime)) &&
                   (str(?date) <= "%TODAY%") &&
                   (regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%")) &&
                   (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
                )
            }

    -   name: "Films"
        description: "Films released on the selected date."
        discovery_map:
            - name
            - uniformName
            - about
        query: |
            PREFIX ont: <http://dbpedia.org/ontology/> 
            SELECT DISTINCT ?entity ?name ?date WHERE {
                ?entity a ont:Film;
                      ont:releaseDate ?date;
                      foaf:name ?name .
                FILTER( 
                   ((datatype(?date) = xsd:date) || (datatype(?date) = xsd:dateTime)) &&
                   (str(?date) <= "%TODAY%") && 
                   (regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%")) && 
                   (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
                )
            }