# Sparql queries go here

queries:
    -   name: "Name of Query"
        description:
        discovery_map:
            - name: "name"
            - creator: "person"
            - about: "person"
        query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?person ?date
			WHERE {
              ?person ont:birthDate ?date .
              ?person a dbpedia-owl:Writer .
              ?person rdfs:label ?name
              FILTER(
                ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) &&
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )&&
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
              )
            }
    -   name: "Athletes"
        description:
        discovery_map:
            - name: "name"
            - creator: "person"
            - about: "person"
        query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?person ?date
			WHERE {
              ?person ont:birthDate ?date .
              ?person a dbpedia-owl:Athlete .
              ?person rdfs:label ?name
              FILTER(
                ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) &&
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )&&
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
              )
            }

    -   name: "Politicians"
        description:
        discovery_map:
            - name: "name"
            - creator: "person"
            - about: "person"
        query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?person ?date  WHERE {
              ?person ont:birthDate ?date .
              ?person a dbpedia-owl:Politician .
              ?person rdfs:label ?name
              FILTER(
                ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) &&
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )&&
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
              )
            }

    -   name: "Musicians"
        description:
        discovery_map:
            - name: "name"
            - creator: "person"
            - about: "person"
        query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?person ?date  WHERE {
              ?person ont:birthDate ?date .
              ?person a dbpedia-owl:MusicalArtist .
              ?person rdfs:label ?name
              FILTER(
                ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) &&
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )&&
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
              )
            }

    -   name: "Countries"
        description:
        discovery_map:
            - name: "country"
        query: |
            SELECT * WHERE {
                ?Country a dbpedia-owl:Country.
                ?Country a yago:Country108544813.
                ?Country dbpedia-owl:foundingDate ?date.
				?Country rdfs:label ?name
                FILTER(
                    ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) &&
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )&&
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
                )
            }

    -   name: "Books"
        description: "Book titles published on the selected date."
        discovery_map:
            - name: "name"
            - about: "name"
        query: |
            PREFIX ont: <http://dbpedia.org/ontology/> 
            SELECT DISTINCT ?book ?name ?date WHERE {
                ?book a ont:Book;
                      ont:publicationDate ?date;
                      rdfs:label ?name .
                FILTER( 
                   ((datatype(?date) = xsd:date) || (datatype(?date) = xsd:dateTime)) &&
                   (str(?date) <= "%TODAY%") && 
                   (regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%")) && 
                   (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en"))
                )
            }