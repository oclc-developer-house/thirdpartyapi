# Sparql queries go here


queries:
    -   name: "Writers"
        description:
        discovery_map:
            - name: "name" 
            - creator: "person"
            - about: "person"
        - query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?person ?date ?name  WHERE { 
              ?person ont:birthDate ?date .
              ?person a dbpedia-owl:Writer .
              ?person rdfs:label ?name
              FILTER( 
                ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) && 
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en")
              )
            }
        name: "Athletes"
        description:
        discovery_map:
            - name: "name" 
            - creator: "person"
            - about: "person"
       - query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?person ?date ?name  WHERE { 
              ?person ont:birthDate ?date .
              ?person a dbpedia-owl:Athlete .
              ?person rdfs:label ?name
              FILTER( 
                ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) && 
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en")
              )
            }
        name: "Politicians"
        description:
        discovery_map:
            - name: "name" 
            - creator: "person"
            - about: "person"
    - query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?person ?date ?name WHERE { 
              ?person ont:birthDate ?date .
              ?person a dbpedia-owl:Politician .
              ?person rdfs:label ?name
              FILTER( 
                ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) && 
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en")
              )
            }
        name: "Musicians"
        description:
        discovery_map:
            - name: "name" 
            - creator: "person"
            - about: "person"
    - query: |
            PREFIX ont: <http://dbpedia.org/ontology/>
            SELECT DISTINCT ?person ?date ?name  WHERE { 
              ?person ont:birthDate ?date .
              ?person a dbpedia-owl:MusicalArtist .
              ?person rdfs:label ?name
              FILTER( 
                ( ( datatype(?date) = xsd:date ) || ( datatype(?date) = xsd:dateTime ) ) && 
                ( str(?date) <= "%TODAY%" ) &&
                ( regex(str(?date), "[0-9]{4}-%MONTH%-%DAY%") )
                (LANG(?name) = "" || LANGMATCHES(LANG(?name), "en")
              )
            }
